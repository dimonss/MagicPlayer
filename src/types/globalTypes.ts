export type NullableString = string | null;
export type NullableNumber = number | null;

// export type RootState = ReturnType<typeof rootReducer>; //todo finished it

export type StringKeyValueI = Record<string, string>;
